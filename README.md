# Vritti Docs Repository

This repository contains the Markdown (`.mdx`) files for the public Vritti API documentation. 
It is powered by [Mintlify](https://mintlify.com/).

To view changes locally:
1. Install Mintlify CLI: `npm i -g mintlify`
2. Run `mintlify dev` in this directory.
